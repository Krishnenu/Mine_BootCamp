const arr=[1,4,6,8,9,0,3,5,2];

function reverseArray(val){
    let i=0;
    if(i===arr.length){
        return;
    }
    reverseArray(aar[i]);
    i++;
    console.log(arr[i]);
} 

reverseArray(arr);